# Test Python in VS Code
print('hello')
print('hi')
import azure.functions as func

import matplotlib.pyplot as plt 
import numpy as np 
import datetime 

# hr         = datetime.datetime.now().hour 

# n          = 100
# hr         = 24 * np.linspace(0, 0.99, n)

# temp_avg   = 32
# temp_delta = 4
# temp_noise = np.random.normal(0, 1, n)/10
# temp_hr    = temp_avg + temp_noise - temp_delta * np.cos(2*np.pi*(hr -3)/24)


# humi_avg   = 75 
# humi_delta = 15 
# humi_noise = np.random.normal(0, 1, n)*5
# humi_hr    = humi_avg + humi_noise + humi_delta * np.cos(2*np.pi*(hr - 4)/24)

# fig, ax    = plt.subplots(1, 2)
# ax[0].plot(hr, temp_hr) 
# ax[1].plot(hr, humi_hr) 
# plt.show()
